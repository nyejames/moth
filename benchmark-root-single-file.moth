message #= "Root single-file benchmark"

render || -> String:
    return [:
        <main>
            <h1>[message]</h1>
            <p>This fixture must stay at the repository root.</p>
        </main>
    ]
;

[render()]
