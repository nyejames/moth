clamp |value Int, minimum Int, maximum Int| -> Int:
    if value < minimum:
        return minimum
    ;
    if value > maximum:
        return maximum
    ;
    return value
;

scale_value |value Int, factor Float| -> Float:
    return value * factor
;

squared |value Int| -> Int:
    return value * value
;
