import @core/constants {app_version}
import @utils/formatting {format_timestamp}

render_footer |timestamp String| -> String:
    return [:
        <footer>
            <p>v[app_version] / [format_timestamp(timestamp)]</p>
        </footer>
    ]
;
