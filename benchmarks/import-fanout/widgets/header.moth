import @core/constants {app_name}
import @utils/formatting {format_label as label}

render_header |title String| -> String:
    return [:
        <header>
            <h1>[label(title)]</h1>
            <span>[app_name]</span>
        </header>
    ]
;
