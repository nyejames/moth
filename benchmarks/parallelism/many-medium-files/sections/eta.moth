EtaCard = | title String, body String |

render_eta || -> String:
    card = EtaCard("Eta", "Seven imported files plus the page cross the parallel file threshold.")
    return [:<article><h2>[card.title]</h2><p>[card.body]</p></article>]
;
