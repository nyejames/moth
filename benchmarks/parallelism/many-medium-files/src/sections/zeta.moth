ZetaCard = | title String, body String |

render_zeta || -> String:
    card = ZetaCard("Zeta", "Runtime template returns keep the frontend path representative.")
    return [:<article><h2>[card.title]</h2><p>[card.body]</p></article>]
;
