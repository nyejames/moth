DeltaCard = | title String, body String |

render_delta || -> String:
    card = DeltaCard("Delta", "Each section is independent and deterministic for merge ordering.")
    return [:<article><h2>[card.title]</h2><p>[card.body]</p></article>]
;
