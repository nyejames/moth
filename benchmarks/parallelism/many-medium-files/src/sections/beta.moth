BetaCard = | title String, body String |

render_beta || -> String:
    card = BetaCard("Beta", "Repeated shape keeps the fixture realistic without adding new syntax.")
    return [:<article><h2>[card.title]</h2><p>[card.body]</p></article>]
;
