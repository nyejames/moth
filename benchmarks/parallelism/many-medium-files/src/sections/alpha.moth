AlphaCard = | title String, body String |

render_alpha || -> String:
    card = AlphaCard("Alpha", "Medium file with a struct, constructor, and runtime template.")
    return [:<article><h2>[card.title]</h2><p>[card.body]</p></article>]
;
