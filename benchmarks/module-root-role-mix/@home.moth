import @api {format_status}
import @toolkit {join_with}

title #= "Module Root Role Mix"

status_text = format_status("active")
body = join_with(status_text, "in the mix")

[:
    <main>
        <h1>[title]</h1>
        <p>[body]</p>
    </main>
]
