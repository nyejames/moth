import @data/theme {Theme, default_theme}

RenderTheme as Theme

render_badge |label String| -> String:
    return [: <span class="badge">[label]</span>]
;

render_theme_name || -> String:
    return default_theme.name
;

