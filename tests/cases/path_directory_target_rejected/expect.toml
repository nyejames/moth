entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0063"]

# Directory targets still use the Stage 0 physical diagnostic, so the path must have an extension.
[[backends.html.diagnostic_assertions]]
code = "MOTH-RULE-0063"
reason = "invalid_compile_time_path.target_is_directory"
count = 1
