entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0063"]

# MOTH-RULE-0063 covers every invalid path literal, so pin the directory reason.
[[backends.html.diagnostic_assertions]]
code = "MOTH-RULE-0063"
reason = "invalid_compile_time_path.target_is_directory"
count = 1
