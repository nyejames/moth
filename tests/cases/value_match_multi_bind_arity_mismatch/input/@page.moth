values_for |flag Bool| -> Int, String:
    number, label = if flag is:
        true => then 1
        else => then 0, "no"
    ;

    return number, label
;

number, label = values_for(true)
io.line([: [number]])
io.line([: [label]])
