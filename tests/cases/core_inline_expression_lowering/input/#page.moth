import @core/math {sin, cos, pow, clamp}
import @core/time {duration_from_seconds, timestamp_from_unix_milliseconds}
import @core/random {random_float}

angle = 1.0
s = sin(angle)
c = cos(angle)
p = pow(2.0, 3.0)
cl = clamp(5.0, 0.0, 10.0)
d = duration_from_seconds(1.5)
t = timestamp_from_unix_milliseconds(1000.0)
r = random_float()
io.line([: [s]])
io.line([: [c]])
io.line([: [p]])
io.line([: [cl]])
io.line([: [r]])
