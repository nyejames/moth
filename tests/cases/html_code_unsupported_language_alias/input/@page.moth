@html codeblock

-- Unsupported language aliases are rejected with a diagnostic that names the
-- offending alias and the supported list.

#[$md:
    [codeblock, $code("rustt"):
        value = 42
    ]
]
