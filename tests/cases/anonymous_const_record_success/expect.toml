[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = [
    "const record label: 42 first: a also: a x: 3 nominal=8 grouped=a panel=Outer detail=Overview",
    "runtime-field=a runtime-nested=3",
    "grouped-call=grouped-call",
]
