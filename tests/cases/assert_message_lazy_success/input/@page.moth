-- a successful assertion must not evaluate its failure message

make_message || -> String:
    io.line("message evaluated")
    return "message"
;

make_condition || -> Bool:
    return true
;

assert(
    make_condition(),
    make_message()
)
io.line("assert_message_lazy_success")
