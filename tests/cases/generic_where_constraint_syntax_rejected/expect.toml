[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0062"]
message_contains = [
    "`where` syntax is not part of Moth generic constraints.",
    "type A is TRAIT",
]
