-- Test that copy() keyword emits __moth_clone_value in the JS backend.
-- Validates deep-copy semantics for collections.

original ~= {1, 2, 3}
cloned = copy(original)
original = cloned
io.line([: [1]])
