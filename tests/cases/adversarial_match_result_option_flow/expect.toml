entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["adversarial_match_result_option_flow sum=75"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["__moth_result_fallback", "while (true)", "switch (", "&&"]
