[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = [
    "fixed_collection_js_runtime lengths=0,1,2,1,2 removed=1 values=2,3"
]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = [
    "__moth_fixed_collection([], 2)",
    "__moth_kind: \"fixed_collection\"",
    "fixedCapacity: fixedCapacity",
    "Fixed collection capacity exceeded",
]
must_not_contain = [
    "__moth_result_propagate(__moth_collection_length(",
]
