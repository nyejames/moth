[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-TYPE-0003"]
message_contains = ["Operator `+` is not available for generic parameter `T`. Moth operators are compiler-owned"]
