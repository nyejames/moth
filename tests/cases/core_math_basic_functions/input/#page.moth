import @core/math {sin, cos, pow, sqrt, clamp}

angle = 0.0
s = sin(angle)
c = cos(angle)
p = pow(2.0, 3.0)
r = sqrt(16.0)
cl = clamp(5.0, 0.0, 10.0)
io.line([: [s]])
io.line([: [c]])
io.line([: [p]])
io.line([: [r]])
io.line([: [cl]])
