entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0041"]
message_contains = ["Receiver methods are not dependency-bound, exported, or aliased independently"]
