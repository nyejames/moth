Pair type Left, Right = |
    left Left,
    right Right,
|

Box type T = |
    value T,
|

IntStringPair as Pair of Int, String

value Box of IntStringPair = Box(Pair(1, "two"))

[: generic alias workaround]
