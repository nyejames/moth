entry = "."

[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["dispatcher_loop_match_result_fallback sum=141"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = ["while (true)", "switch (", "__moth_result_fallback", "const __match_"]
