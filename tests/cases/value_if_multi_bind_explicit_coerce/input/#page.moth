get_values |condition Bool| -> Float, String:
    first Float, second = if condition then 1, "yes" else 0, "no"
    return first, second
;

a, b = get_values(true)
io.line("coerce")
io.line([: [a]])
io.line([: [b]])
