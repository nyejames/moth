-- Entry file for multi-file module test
-- Should change some of the import syntax later when it supports struct files
import @helper { greet }

io.line("Starting main")
greet("World")
io.line("Main complete")