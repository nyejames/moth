-- A map literal stores a non-copy key through shared storage; the source remains usable.
key ~= "Priya"
scores ~{String = Int} = {key = 10}
stored = scores.get(key) catch:
    then 0
;
[:map_literal_stored_non_copy_key_later_use key=[key] stored=[stored]]
