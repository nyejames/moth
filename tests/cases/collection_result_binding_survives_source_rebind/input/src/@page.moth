Point = |
    x Int,
|

values ~= {Point(1)}
returned ~= values.get(0) catch:
    then Point(0)
;

returned = Point(2)
returned.x = 3

~values.set(0, Point(4)) catch:
;

stored = values.get(0) catch:
    then Point(0)
;

io.line([:returned=[returned.x] stored=[stored.x]])
