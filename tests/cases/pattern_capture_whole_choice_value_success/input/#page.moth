Status :: Ready, Busy;

status = Status::Busy
result ~= ""

if status is:
    Ready => result = "ready"
    captured => result = "captured"
    else => result = "else"
;

[:pattern_capture_whole_choice_value_success result=[result]]
