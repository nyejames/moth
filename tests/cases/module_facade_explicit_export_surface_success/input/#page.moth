import @widget {
    PublicCard,
    PublicState,
    PublicText,
    make_state,
    public_title,
    render_card,
}

card = PublicCard(public_title)
state PublicState = make_state()
text PublicText = render_card(card)

[:module_facade_explicit_export_surface_success [text]]
