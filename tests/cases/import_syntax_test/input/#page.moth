-- Test import syntax and entry point execution
import @utils/math { add }
import @utils/string { format_number }

result = add(5, 3)
formatted = format_number(result)
io.line([: Result: [formatted]])
