[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0041"]
message_contains = ["Receiver methods on instantiated generic receiver types are not supported", "using the receiver type's own parameters"]
