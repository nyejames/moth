[backends.html]
mode = "success"
warnings = "forbid"
rendered_output_contains = ["collection_mutable_access_js_regression first=99 removed=9 len=1"]

[[backends.html.artifact_assertions]]
path = "index.html"
kind = "html"
must_contain = [
    "__moth_collection_push(",
    "__moth_collection_set(",
    "__moth_collection_remove(",
    "__moth_collection_get(",
]
must_not_contain = [
    "__moth_write(__moth_index(",
    "__moth_result_propagate(__moth_collection_push(",
    "__moth_result_propagate(__moth_collection_remove(",
    "__moth_result_propagate(__moth_collection_length(",
]
