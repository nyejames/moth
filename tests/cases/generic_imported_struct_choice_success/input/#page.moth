import @types { Box }
import @types { ResultShape }

num_box Box of Int = Box(42)
str_box Box of String = Box("hello")

result ResultShape of String, Error = ResultShape::Ok("world")

io.line([: [str_box.value]])

[: generic imported success]
