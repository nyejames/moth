[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0054"]

message_contains = ["requires mutable access for parameter 'value'", "but `x` is immutable", "Declare the binding as mutable", "then pass `~x`"]

[[backends.html.diagnostic_assertions]]
code = "MOTH-RULE-0054"
reason = "invalid_call_shape.mutable_access_on_immutable_place"
