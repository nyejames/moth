@helper2 sine, CIRCLE_RATIO, Span, make_span, span_seconds

span Span = make_span(2.5)
seconds = span_seconds(span)
io.line([:transitive_binding sine=[sine(0.0)] ratio=[CIRCLE_RATIO] seconds=[seconds]])
