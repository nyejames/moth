entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-SYNTAX-0025"]
message_contains = ["A standalone template is not a valid statement here. Assign it, return it or pass it to a receiving expression."]
