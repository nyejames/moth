canvas #= [:<canvas id="[$slot("id")]" style="[$slot("style")]"></canvas>]

output = [canvas:
    [$insert("id"):canvas]
    [$insert("style"):
        position: fixed;
        inset: 0;
    ]
]

[output]
