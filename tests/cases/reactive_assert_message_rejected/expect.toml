entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0046"]
message_contains = ["Assertion messages must be string literals"]

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0046"]
message_contains = ["Assertion messages must be string literals"]
