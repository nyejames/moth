-- Test that struct field mutation routes through the JS computed-place layer.
-- Validates that __moth_field and __moth_write are used for field assignment.

Point = |
    x Int,
    y Int,
|

p ~= Point(1, 2)
p.x = 10
p.y = 20
io.line([: Point: ([p.x], [p.y])])
