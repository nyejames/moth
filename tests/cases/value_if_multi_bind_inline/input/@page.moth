get_values |condition Bool| -> Int, String:
    first, second = if condition then 1, "yes" else 0, "no"
    return first, second
;

a, b = get_values(true)
io.line("inline")
io.line([: [a]])
io.line([: [b]])
