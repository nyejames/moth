import @cards { PUBLIC_DISPLAY, render as show }

Label = |
    text String,
|

display |this Label| -> String:
    return this.text
;

Label must PUBLIC_DISPLAY

[:trait_bound_facade_grouped_wrapper_success [show(Label("facade"))]]
