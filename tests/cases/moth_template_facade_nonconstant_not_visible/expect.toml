entry = "."

[backends.html]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0034"]
diagnostic_match = "contains"
diagnostic_match_reason = "Current pre-canonical directory frontend compiles the imported child module as both a root candidate and a reachable dependency."

[backends.html_wasm]
mode = "failure"
warnings = "forbid"
diagnostic_codes = ["MOTH-RULE-0034"]
diagnostic_match = "contains"
diagnostic_match_reason = "Current pre-canonical directory frontend compiles the imported child module as both a root candidate and a reachable dependency."
