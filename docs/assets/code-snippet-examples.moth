
-- let's stop trying to reinvent markdown, and just build on it
content #String = [$md:
    ### Markdown inside templates

    Moth includes its own *compact*, *template-aware* flavour of Markdown
    through the built-in `$md` formatter.

    [$note: this is how you write comments!]
]








