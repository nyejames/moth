@runtime-and-backend-lowering content as reference_content
@styles navbar, section, theme_head

page_title #= "Runtime and backend memory lowering"
page_description #= "How physical memory strategies and the collector-free release guarantee meet backend targets."
page_head #= theme_head

#[navbar]

#[section, $md:
    @../ (Memory management) / Runtime and backend memory lowering

    Different targets and profiles may receive different allocation and GC representations. The compiler-owned memory planner selects them per physical variant, and no backend chooses different language semantics.
]

#[section, reference_content]
