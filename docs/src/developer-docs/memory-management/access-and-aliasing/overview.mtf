# Access and aliasing

Shared reads, explicit copies and `~` access.

**Moth is reference-semantic by default, copy-explicit and move-inferred. It omits explicit reference types and lifetime syntax, not references themselves.**

## Contract

- Input: source access syntax and binding/argument forms
- Output: validated shared, exclusive and copy semantics
- Decides: what counts as shared access, when `copy` is required, where `~` is valid, and how mutable aliases behave
- Must not decide: borrow-checker implementation, lifetime-topology validation, backend allocation, or ownership transfer
- Invariant: existing values use shared read-only access by default; mutation requires explicit exclusive access

## Main paths

- `src/compiler_frontend/value_mode.rs` (current navigation aid only, not design authority)
- `src/compiler_frontend/ast/expressions/`
- `src/compiler_frontend/ast/statements/`
- `src/compiler_frontend/ast/field_access/`

## Read next

- `docs/src/developer-docs/memory-management/access-and-aliasing/access-and-aliasing.mtf`
- `docs/src/developer-docs/memory-management/lifetime-regions-and-escape-validation/overview.mtf`
- `docs/compiler-design-overview.md`, especially Stage 4 and Stage 5
