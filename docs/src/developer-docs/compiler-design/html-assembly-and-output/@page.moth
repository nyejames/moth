@html-assembly-and-output content as reference_content
@styles navbar, section, theme_css
@diagrams css as diagrams_css, diagram as flow_diagram, stage as flow_stage, node as flow_node, branch as flow_branch, lane as flow_lane, detail as flow_detail, island as flow_island, fanout as flow_fanout

page_title #= "18. HTML assembly and output"
page_description #= "How the HTML builder assembles and owns the generated page, its resources and its output manifest."
page_head #= [:<style>[theme_css] [diagrams_css]</style>]

#[navbar]

#[section, $md:
    @../ (Compiler design) / 18. HTML assembly and output

    The HTML builder assembles records into a document and owned output set, ending at the greeting fragment.
]

#[section, reference_content]

#[section, $md:
    @../ (Compiler design) / @../backend-lowering (Previous)
]

export:
    diagram #= flow_diagram
    stage #= flow_stage
    node #= flow_node
    branch #= flow_branch
    lane #= flow_lane
    detail #= flow_detail
    island #= flow_island
    fanout #= flow_fanout
;
