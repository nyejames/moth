@moth-design-choices content as reference_content
@styles navbar, section, theme_css
@diagrams css as diagrams_css, diagram as flow_diagram, stage as flow_stage, node as flow_node, branch as flow_branch, lane as flow_lane, detail as flow_detail, island as flow_island, fanout as flow_fanout

page_title #= "02. Why Moth makes different choices"
page_description #= "The design trade-offs behind Moth's language and compiler."
page_head #= [:<style>[theme_css] [diagrams_css]</style>]

#[navbar]

#[section, $md:
    @../ (Compiler design) / 02. Why Moth makes different choices

    Moth keeps the source language small and moves complexity into the compiler on purpose. This article names the bias before the pipeline walk begins.
]

#[section, reference_content]

#[section, $md:
    @../ (Compiler design) / @../what-a-compiler-does (Previous) / @../starting-a-build (Next)
]

export:
    diagram #= flow_diagram
    stage #= flow_stage
    node #= flow_node
    branch #= flow_branch
    lane #= flow_lane
    detail #= flow_detail
    island #= flow_island
    fanout #= flow_fanout
;
