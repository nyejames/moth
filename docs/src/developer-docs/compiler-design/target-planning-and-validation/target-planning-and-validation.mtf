# Deciding where code can run

Target planning receives an already-validated lifetime topology. It assigns targets and checks capabilities. It never changes region ownership or source legality.

Semantically valid Moth can still be unhostable on a chosen target. A function that touches the DOM does not belong in a pure Wasm partition that has no DOM. A backend should learn that **before** it emits half a module.

**Target planning** is build-owned and assigns each reachable function to a target. **Target validation** is compiler-owned and checks that finished assignment against capabilities.

## Roots and capabilities

The build system supplies **validation roots** and the completed per-function target assignment from entry or package link planning. The compiler owns validation semantics over that assignment. It judges the partition rather than choosing it.

Distinguish two ideas people conflate:

- **package provenance** records where a fact came from. `PackageOrigin` says which kind of package it belongs to and `PackageBacking` says whether real Moth source or an external binding implements it.
- **target capability** is builder and backend metadata describing which semantic features, bindings and helpers a target supports, plus what each function requires.

Neither one is source-visible target identity. Moth source cannot ask which physical target it is being compiled for.

A function can be perfectly typed and still require a capability its assigned target lacks.

For the greeting walkthrough, `start`, `greet` and any template runtime work arrive with link facts and capability requirements. Even a humble string page exercises the machinery that larger apps need for the DOM and everything around it.

## Affinity and propagation

Some functions are born with **affinity**. A JavaScript-owned `start` that must orchestrate page lifecycle wants the JS side. A DOM-touching helper wants JS. Neutral pure computation may default toward Wasm where the design supports it.

Requirements **propagate backwards** along call edges. If a callee needs the DOM, callers that must reach it inherit the constraint under the accepted propagation rules. Neutral callees can still sit in Wasm when callers bridge correctly.

[diagram:
    [$insert("label"): A capability requirement travels backwards along call edges]
    [$insert("caption"): The helper needs the DOM, so anything that must reach it inherits the requirement. Constraints climb towards callers, which is why a caller can be moved by a decision made three calls away.]

    [stage:
        [node:
            [$insert("href"): ./]
            [$insert("eyebrow"): Callee]
            [$insert("title"): Touches the DOM]
            This helper can only run where a DOM exists, so it is JavaScript-bound.
        ]
    ]

    [stage:
        [node:
            [$insert("href"): ./]
            [$insert("eyebrow"): Caller]
            [$insert("title"): Inherits the requirement]
            A caller that must reach the helper picks up the same constraint.
        ]
    ]

    [stage:
        [node:
            [$insert("href"): ../linking-entries-and-reachability/]
            [$insert("eyebrow"): Root]
            [$insert("title"): start lands in JavaScript]
            The requirement surfaces at the entry. Neutral callees stay in Wasm where callers bridge correctly.
        ]
    ]
]

## Deterministic assignment

A clean mental loop:

1. take the reachable union and validated lifetime topology
2. run affinity/capability analysis
3. assign targets
4. record reasons
5. validate assigned functions
6. validate cross-target edges

Determinism matters. The same program should not flip partitions because a hash map iterated in a different order.

## Cross-target calls

Mixed output needs bridges.

**JavaScript-to-Wasm** calls may use wrappers under explicit ABI rules.

**Wasm-to-JavaScript Moth calls** remain prohibited after propagation in the accepted model. The planner should push the JS requirement outward rather than leave a forbidden reverse edge for emission to discover.

That asymmetry is deliberate engineering, not an accident of today's engine.

## Entry-specific variants and check

Partitioning is **entry-specific**. Different entries may need different physical variants of shared logic when their capability sets differ. Variant keys make those physical artefacts addressable.

The `check` command plans and validates, then stops before writing a full page output set. Tooling overlays extend analysis. They do not become a second artefact builder competing with HTML output.

## What can fail here

- unsupported target feature on a reachable function
- missing capability
- invalid cross-target edge
- whole-module HTML-Wasm behaviour still marked transitional or experimental in the progress matrix

Unresolved layout requirements are not on that list. Layout and memory-strategy planning happens per variant *after* target validation, so those failures belong to the memory planner.

Unsupported features in unreachable private functions should not fail validation.

## Why Moth does it this way

**Alternative:** compile a whole page to one target, or let emission discover unsupported calls mid-write.

**Choice:** per-function deterministic partitioning with explicit validation.

**Benefit:** mixed output can keep browser work in JavaScript and neutral work in Wasm.

**Cost:** wrappers, ABI rules and assignment reasons add planning machinery.

## Roadmap and current status

- **Accepted design:** entry-specific target assignment and compiler-owned target validation.
- **Current implementation:** progress matrix for mixed-target coverage.
- **Queued work:** `html_project_backend_wasm_final_implementation_plan.md`.
- **Deferred or outside scope:** whole-module target selection as the final model.

## Exit state

You leave with validated target assignments, capabilities, dependencies and physical variants. Next: how lowerers turn these explicit inputs into JavaScript and Wasm without reopening source.
