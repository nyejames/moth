# Finding the program's shape

Bodies can wait. Before the compiler checks whether `greet(visitor)` type-checks, it needs the module's top-level shape: which declarations exist, which dependencies wait on providers and which local names depend on which other local names.

Header work finds that shape **once**. Later stages bind and consume retained syntax. They do not reparse the file from characters whenever a provider finishes.

## Prepared shells

From located tokens, syntax preparation extracts **shells**:

- declaration shells for constants, functions, structs, choices, aliases, traits and conformances
- dependency shells, direct selections and aliases
- const-template, active-root start-body and trait-incompatibility shells
- root-activity and fragment-placement metadata for normal roots
- structural provider references and structural file references for the graph
- source `#Config` contract shells
- local declaration-ordering hints
- source locations and remap information

For the greeting source, shells include something like "constant `greeting_prefix`" and "function `greet` with these parameter and return type shells." For the page, preparation keeps the `@greeting` dependency selection shell, the start-body token stream that contains `visitor` and the fragment-placement metadata around the HTML template.

Note what has *not* happened yet. `greet` is not bound to anything, and `visitor` is not a declaration the header stage understands. Binding resolves the first later, and AST parses the second out of the start body.

Preparation does not type-check executable bodies. It does not open source provider interfaces. It records what source syntax already makes knowable without them.

## Four relationship classes

Header processing keeps four edge families distinct. Mixing them is a reliable way to build a confusing compiler. Each authored path row has exactly one role.

**Structural provider references** belong to Stage 0 graph construction. They answer "which module or package must exist before this consumer can bind?"

**Structural file references** also belong to Stage 0, plus physical-input resolution. They answer "which file on disk does this path point at?"

**Dependency-bound symbol bindings** belong to visibility and AST. They answer "which public names from a provider become usable here, under which local spellings?"

**Local declaration-ordering edges** belong to Stage 3 inside one module. They answer "which top-level declarations must be processed before which others in this module?"

A dependency-bound declaration is never a node in the consumer's local declaration graph. Copying provider declarations into each consumer would destroy identity and explode work.

[diagram:
    [$insert("label"): Four kinds of edge, four owners]
    [$insert("caption"): One authored path has exactly one role. When a design discussion gets confusing, it is usually because two of these four are being called the same thing.]

    [stage:
        [node:
            [$insert("href"): ../project-graphs-and-modules/]
            [$insert("eyebrow"): Stage 0]
            [$insert("title"): Structural references]
            Provider references ask which module must exist first. File references ask which file on disk a path points at.
        ]
    ]

    [stage:
        [node:
            [$insert("href"): ../ast-semantics-and-types/]
            [$insert("eyebrow"): Visibility and AST]
            [$insert("title"): Dependency symbol bindings]
            Which public names from a provider become usable here, and under which local spellings.
        ]
    ]

    [stage:
        [node:
            [$insert("href"): ./]
            [$insert("eyebrow"): Stage 3]
            [$insert("title"): Local ordering edges]
            Which top-level declarations inside this one module must be processed before which others.
        ]
    ]
]

*A dependency-bound declaration never becomes a node in the consumer's local graph. Copying provider declarations into every consumer would destroy identity and multiply the work.*

## Binding retained dependencies

`PreparedHeaderSyntax` exists **before** providers compile. Dependency shells wait with their source locations intact.

In the walkthrough both files live in one directory-scoped module, so `greeting.moth` is owned source rather than a separate provider. The module compiles once and the retained `@greeting` clause binds inside it. Provider-first scheduling is for the other case, where a dependency resolves to a different module or package.

Either way, **interface binding** resolves retained dependency shells against a finished public interface:

- stable bound declaration identities
- bound canonical type and folded-value facts
- final file-local visibility
- namespace records and collision results

Binding does not retokenize. It does not reparse declaration syntax. It fills in what only a finished provider can supply.

Before binding, `greet` is a name in a dependency shell. After binding, it is a stable origin identity with known type facts the AST can trust.

## Local declaration ordering

Inside one module, Stage 3 builds a **declaration dependency DAG** from retained local edges and sorts it.

Edges include facts AST needs before linear consumption:

- local type alias targets
- local struct and choice field type references
- local function parameter and return type references
- local explicit constant type references
- fixed collection capacities that use local constants
- local constant initializer references
- local trait and conformance ordering where required

**Function-body references do not create ordering edges.** Bodies may call functions declared later in the file. Only the top-level shape needs a stable process order.

Sorting uses stable tie-breaking so independent declarations keep their source order. Same-file constants keep source-order semantics. Same-file forward references among constants are rejected. Cross-module constants arrive already folded in provider interfaces.

Dormant `start` appends after declarations. It never participates as a dependency node that other declarations wait on.

## Cycles and visibility

If two local declarations require each other in a way the edge set captures, the sort stops and a cycle diagnostic fires.

Visibility rules stay strict. User-visible names do not silently shadow one another across same-file declarations, dependencies, aliases, prelude symbols and builtins. One collision policy covers the surface.

Dependency-bound declarations influence visibility after binding. They still do not become local graph nodes for Stage 3.

## What can fail here

- malformed declaration shell or dependency shape
- visibility collision or missing provider symbol
- local declaration cycle or invalid forward reference
- support roots or facades that illegally carry root runtime activity

Final prose and rendering of diagnostics wait for the diagnostics article. Here the failures are structural.

## Why Moth does it this way

**Alternative:** reparse source after providers compile, or copy declarations into each consumer.

**Choice:** parse and retain syntax once, then bind immutable provider interfaces.

**Benefit:** source locations, identities and graph scheduling stay aligned. Parallel workers can prepare shells early and bind later.

**Cost:** prepared syntax carries more metadata across the build boundary.

## Roadmap and current status

- **Accepted design:** `PreparedHeaderSyntax`, `BoundModuleHeaders` and distinct relationship classes.
- **Current implementation:** header and dependency progress rows.
- **Queued work:** no named roadmap plan changes these contracts. Canonical module compilation, dependency binding and typed project values already landed. Use the progress matrix for remaining partial coverage.
- **Deferred or outside scope:** this page does not teach body type checking.

## Exit state

You leave with bound visibility, stable dependency facts and ordered declarations. Next: how syntax acquires checked semantic meaning in the AST.
