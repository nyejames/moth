import @blocks-and-statements {
    content as blocks_and_statements,
}

import @blocks-and-statements-basic {
    content as blocks_and_statements_basic,
}

import @comments-and-naming {
    content as comments_and_naming,
}

import @comments-and-naming-basic {
    content as comments_and_naming_basic,
}

import @core-values {
    content as core_values,
}

import @core-values-basic {
    content as core_values_basic,
}

import @strings-and-characters {
    content as strings_and_characters,
}

import @strings-and-characters-basic {
    content as strings_and_characters_basic,
}

import @styles {
    navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager,
}

page_title #= "Language basics"
page_description #= "Blocks, comments, naming, core values, strings and characters in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Language basics

    # Language basics

    Moth uses a compact set of syntax rules across the whole language. Colons
    open blocks, semicolons close them and square brackets build strings rather
    than index collections.

    These choices differ from C-like and Python-like languages, but they keep
    each delimiter tied to one job. Start here before moving to bindings,
    control flow and user-defined types.
]

#[section:
    [doc_level:
        [$insert("key"):blocks-and-statements]
        [$insert("title"):Blocks and statements]
        [$insert("advanced"):[blocks_and_statements]]

        [blocks_and_statements_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):comments-and-naming]
        [$insert("title"):Comments and naming]
        [$insert("advanced"):[comments_and_naming]]

        [comments_and_naming_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):core-values]
        [$insert("title"):Core values]
        [$insert("advanced"):[core_values]]

        [core_values_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):strings-and-characters]
        [$insert("title"):Strings and characters]
        [$insert("advanced"):[strings_and_characters]]

        [strings_and_characters_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../getting-started/]
        [$insert("previous_title"):Getting Started]
        [$insert("next_path"):../bindings/]
        [$insert("next_title"):Values and bindings]
    ]
]
