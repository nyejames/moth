import @./starting-a-build {content as reference_content}
import @styles/docs {navbar, section, theme_head}

page_title #= "03. Starting a build"
page_description #= "How command selection and project config prepare a Moth build."
page_head #= theme_head

#[navbar]

#[section, $md:
    @../ (Compiler design) / 03. Starting a build

    # A build starts before source compilation

    A build chooses its command, builder and folded project values before any page source enters the compiler.
]

#[section, reference_content]

#[section, $md:
    @../ (Compiler design) / @../moth-design-choices (Previous) / @../project-graphs-and-modules (Next)
]
