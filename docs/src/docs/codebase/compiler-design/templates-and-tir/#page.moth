import @templates-and-tir {content as reference_content}
import @styles {navbar, section, theme_head}

page_title #= "09. Templates and TIR"
page_description #= "How Moth preserves template structure before runtime lowering."
page_head #= theme_head

#[navbar]

#[section, $md:
    @../ (Compiler design) / 09. Templates and TIR

    # Why templates need their own temporary IR

    First-class templates keep structure in AST-local TIR, then hand folded strings or neutral runtime data downstream.
]

#[section, reference_content]

#[section, $md:
    @../ (Compiler design) / @../compile-time-semantics (Previous) / @../diagnostics (Next)
]
