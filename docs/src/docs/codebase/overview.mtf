# Codebase documentation

`docs/src/docs/codebase/**` owns memory design, implementation style, testing and validation.

`docs/src/docs/codebase/language/overview.mtf` routes contributors to the
canonical focused references for source syntax and observable language
semantics.

Use the progress matrix for current support. Use the roadmap for sequencing, active plans and genuinely deferred design.

## Core reading

- `docs/src/docs/codebase/style-guide/style-guide.mtf`
- `docs/src/docs/codebase/memory-management/overview.mtf`

Read `docs/src/docs/codebase/style-guide/validation.mtf` before completing or
reviewing a non-trivial change.

Read `docs/src/docs/codebase/style-guide/testing.mtf` when work changes
behaviour, diagnostics, compiler stages, backends, tests or fixtures.

For compiler architecture and stage ownership, use
`docs/compiler-design-overview.md`, the canonical technical design reference.

For design-boundary questions, read the public Design Scope authority at
`docs/src/docs/design-scope/`.

For build-system or project orchestration questions, read `docs/build-system-design.md`.

## Compiler flow

[codeblock, $code("text"):
    command and Stage 0 build orchestration
        -> tokenization and header syntax preparation
        -> provider graph compilation
        -> interface binding and local declaration ordering
        -> AST
        -> HIR and validation
        -> borrow validation
        -> lifetime-region and escape validation
        -> build-owned link planning and backend lowering
]

Keep detailed contracts in `docs/compiler-design-overview.md` and `docs/build-system-design.md`.

## Core architectural rules

- User-facing source, syntax, import, type, rule, borrow, config and deferred-feature failures use `CompilerDiagnostic`. `CompilerError` is for internal compiler, filesystem, backend, dev-server and tooling failures.
- Stage 0 builds one canonical module graph, assigns file ownership and compiles each module once in dependency order.
- Each module owns one local `TypeEnvironment`. Use local `TypeId` equality for module-local decisions and canonical project identities across module interfaces.
- Header parsing owns top-level declaration discovery and declaration shells. AST consumes sorted headers and header-built visibility.
- Import preparation builds the visibility environment used by dependency sorting and AST. Expression and type resolution use the active `ScopeContext`.
- Generics, traits and explicit cast evidence resolve before HIR.
- AST owns template composition, constant folding, slot routing, runtime handoff preparation and compile-time page fragment metadata. TIR remains AST-local.
- HIR is the first backend-facing semantic IR. It makes control flow and cross-module call targets explicit.
- Borrow validation reads HIR and writes side-table facts. It doesn't mutate HIR or decide final runtime ownership.
- Lifetime-region and escape validation is mandatory after borrow validation. GC cannot bypass topology legality.
- GC is the semantic baseline representation for already-legal topology. Ownership and deterministic destruction are optimisation layers.
- **Moth is reference-semantic by default, copy-explicit and move-inferred. It omits explicit reference types and lifetime syntax, not references themselves.**
- Existing values use shared, read-only access by default. Copies are explicit, `~` requests exclusive access and ownership transfer remains compiler-inferred and optional.
- Backend builders consume the compiled project graph, entry or package link plans, validated module HIR, paired type environments, borrow facts and lifetime facts. They don't rediscover source imports or reinterpret source syntax.

## Design in one minute

- Keep the source language small, explicit and statically inspectable.
- Prefer constrained mechanisms over general metaprogramming.
- Prefer static reuse through generics and trait bounds.
- Use choices for runtime heterogeneity.
- Keep ownership and backend optimisation behind stable source semantics.
- Distinguish unimplemented design from intentionally excluded design.

See `docs/src/docs/design-scope/` for the high-level rationale and
`docs/compiler-design-overview.md` for concrete compiler architecture.
