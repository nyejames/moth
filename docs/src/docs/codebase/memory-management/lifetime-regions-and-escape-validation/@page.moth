import @lifetime-regions-and-escape-validation {content as reference_content}
import @styles {navbar, section, theme_head}

page_title #= "Lifetime regions and escape validation"
page_description #= "How Moth validates lifetime owners, retained references, escapes and region topology."
page_head #= theme_head

#[navbar]

#[section, $md:
    @../ (Memory management) / Lifetime regions and escape validation

    Every allocation has exactly one semantic lifetime owner. GC cannot bypass topology validation.
]

#[section, reference_content]
