@template-basics content as template_basics

@template-basics-basic content as template_basics_basic

@template-directives content as template_directives

@template-directives-basic content as template_directives_basic

@template-slots content as template_slots

@template-slots-basic content as template_slots_basic

@child-wrappers content as child_wrappers

@child-wrappers-basic content as child_wrappers_basic

@template-control-flow content as template_control_flow

@template-control-flow-basic content as template_control_flow_basic

@markdown-formatting content as markdown_formatting

@markdown-formatting-basic content as markdown_formatting_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Templates"
page_description #= "Template strings, directives, slots, wrappers, control flow and Markdown in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Templates

    # Templates

    Templates form Moth's only string-composition system. Square brackets can
    join short values, format Markdown or HTML and compose larger layouts with
    slots, wrappers and control flow.

    Other languages often split this work across concatenation, interpolation
    and a separate template language. Moth keeps one typed `String` result and
    lets the compiler fold it when every input comes from compile time.
]

#[section:
    [doc_level:
        [$insert("key"):template-basics]
        [$insert("title"):Template basics]
        [$insert("advanced"):[template_basics]]

        [template_basics_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):template-directives]
        [$insert("title"):Template directives]
        [$insert("advanced"):[template_directives]]

        [template_directives_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):template-slots]
        [$insert("title"):Template slots]
        [$insert("advanced"):[template_slots]]

        [template_slots_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):child-wrappers]
        [$insert("title"):Child wrappers]
        [$insert("advanced"):[child_wrappers]]

        [child_wrappers_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):template-control-flow]
        [$insert("title"):Template control flow]
        [$insert("advanced"):[template_control_flow]]

        [template_control_flow_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):markdown-formatting]
        [$insert("title"):Markdown formatting]
        [$insert("advanced"):[markdown_formatting]]

        [markdown_formatting_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../collections/]
        [$insert("previous_title"):Collections and maps]
        [$insert("next_path"):../constants/]
        [$insert("next_title"):Constants and compile-time behaviour]
    ]
]
