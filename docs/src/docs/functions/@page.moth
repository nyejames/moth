@function-declarations content as function_declarations

@function-declarations-basic content as function_declarations_basic

@parameters-and-defaults content as parameters_and_defaults

@parameters-and-defaults-basic content as parameters_and_defaults_basic

@calls-and-access content as calls_and_access

@calls-and-access-basic content as calls_and_access_basic

@returns-and-multiple-values content as returns_and_multiple_values

@returns-and-multiple-values-basic content as returns_and_multiple_values_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Functions"
page_description #= "Function declarations, parameters, calls, access and return values in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Functions

    # Functions

    Moth functions use named declarations rather than anonymous closures or
    general function values. A signature records each parameter's type and
    access mode, then lists every success and error return.

    Calls stay local and explicit. The source shows named arguments, mutable
    access and failure propagation where they occur.
]

#[section:
    [doc_level:
        [$insert("key"):function-declarations]
        [$insert("title"):Function declarations]
        [$insert("advanced"):[function_declarations]]

        [function_declarations_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):parameters-and-defaults]
        [$insert("title"):Parameters and defaults]
        [$insert("advanced"):[parameters_and_defaults]]

        [parameters_and_defaults_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):calls-and-access]
        [$insert("title"):Calls and access]
        [$insert("advanced"):[calls_and_access]]

        [calls_and_access_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):returns-and-multiple-values]
        [$insert("title"):Returns and multiple values]
        [$insert("advanced"):[returns_and_multiple_values]]

        [returns_and_multiple_values_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../casts/]
        [$insert("previous_title"):Casts]
        [$insert("next_path"):../branching/]
        [$insert("next_title"):Branching]
    ]
]
