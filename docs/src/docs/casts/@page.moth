import @cast-syntax {
    content as cast_syntax,
}

import @cast-syntax-basic {
    content as cast_syntax_basic,
}

import @cast-targets {
    content as cast_targets,
}

import @cast-targets-basic {
    content as cast_targets_basic,
}

import @fallible-casts {
    content as fallible_casts,
}

import @fallible-casts-basic {
    content as fallible_casts_basic,
}

import @cast-evidence {
    content as cast_evidence,
}

import @cast-evidence-basic {
    content as cast_evidence_basic,
}

import @styles {
    navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager,
}

page_title #= "Casts"
page_description #= "Explicit cast syntax, target contexts, fallible conversion and cast evidence in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Casts

    # Casts

    Casts stay explicit and target-driven. The receiving type says where the value goes. `cast`, `cast!` or `catch` says how the program handles conversion failure.

    This keeps conversion local. You can see the source, target and failure path without asking the compiler to guess from distant code.
]

#[section:
    [doc_level:
        [$insert("key"):cast-syntax]
        [$insert("title"):Cast syntax]
        [$insert("advanced"):[cast_syntax]]

        [cast_syntax_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):cast-targets]
        [$insert("title"):Cast targets]
        [$insert("advanced"):[cast_targets]]

        [cast_targets_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):fallible-casts]
        [$insert("title"):Fallible casts]
        [$insert("advanced"):[fallible_casts]]

        [fallible_casts_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):cast-evidence]
        [$insert("title"):Cast evidence]
        [$insert("advanced"):[cast_evidence]]

        [cast_evidence_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../numbers/]
        [$insert("previous_title"):Numbers]
        [$insert("next_path"):../functions/]
        [$insert("next_title"):Functions]
    ]
]
