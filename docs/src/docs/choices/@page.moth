@choice-declarations content as choice_declarations

@choice-declarations-basic content as choice_declarations_basic

@variant-construction content as variant_construction

@variant-construction-basic content as variant_construction_basic

@payload-patterns content as payload_patterns

@payload-patterns-basic content as payload_patterns_basic

@choice-equality content as choice_equality

@choice-equality-basic content as choice_equality_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Choices"
page_description #= "Choice declarations, variants, payload patterns and structural equality in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Choices

    # Choices

    A choice creates a nominal tagged union. Each value carries one named variant
    with an optional record payload, much like an enum with data in Rust.

    Pattern matching opens payloads without nullable fields or unchecked casts.
    Equality remains available only when every possible payload type supports it.
]

#[section:
    [doc_level:
        [$insert("key"):choice-declarations]
        [$insert("title"):Choice declarations]
        [$insert("advanced"):[choice_declarations]]

        [choice_declarations_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):variant-construction]
        [$insert("title"):Variant construction]
        [$insert("advanced"):[variant_construction]]

        [variant_construction_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):payload-patterns]
        [$insert("title"):Payload patterns]
        [$insert("advanced"):[payload_patterns]]

        [payload_patterns_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):choice-equality]
        [$insert("title"):Choice equality]
        [$insert("advanced"):[choice_equality]]

        [choice_equality_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../structs/]
        [$insert("previous_title"):Structs]
        [$insert("next_path"):../errors/]
        [$insert("next_title"):Errors, options and assertions]
    ]
]
