import @./prelude {
    content as prelude_advanced,
}

import @./prelude-basic {
    content as prelude_basic,
}

import @styles/docs {
    navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager,
}

page_title #= "Prelude policy"
page_description #= "The small implicit surface available without explicit imports."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../../../ (Documentation) / @../ (Core packages) / Prelude policy

    # Prelude policy

    The prelude gives you a few names without an import. It keeps common
    output convenient without making every Core package global.
]

#[section:
    [doc_level:
        [$insert("key"):prelude-surface]
        [$insert("title"):Prelude surface]
        [$insert("advanced"):[prelude_advanced]]

        [prelude_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../]
        [$insert("previous_title"):Core packages]
        [$insert("next_path"):../io/]
        [$insert("next_title"):Core IO]
    ]
]