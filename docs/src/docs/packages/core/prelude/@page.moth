@prelude content as prelude_advanced

@prelude-basic content as prelude_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Prelude policy"
page_description #= "The small implicit surface available without explicit dependency clauses."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../../../ (Documentation) / @../ (Core packages) / Prelude policy

    # Prelude policy

    The prelude registers the bare `io` namespace without a dependency clause. The
    compiler-owned `Error` type also remains available without a dependency clause, but it
    is a builtin rather than a prelude symbol. Most Core capabilities still
    require visible dependency bindings.
]

#[section:
    [doc_level:
        [$insert("key"):prelude-surface]
        [$insert("title"):Prelude surface]
        [$insert("advanced"):[prelude_advanced]]

        [prelude_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../]
        [$insert("previous_title"):Core packages]
        [$insert("next_path"):../io/]
        [$insert("next_title"):Core IO]
    ]
]
