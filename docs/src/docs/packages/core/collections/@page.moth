import @collections {
    content as collections_advanced,
}

import @collections-basic {
    content as collections_basic,
}

import @styles {
    navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager,
}

page_title #= "Core collections"
page_description #= "Ordered collection literals and common collection methods."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../../../ (Documentation) / @../ (Core packages) / Core collections

    # Core collections

    Moth collections store ordered values through language-owned literals and
    methods backed by `@core/collections`. The package remains implicit because
    collection syntax belongs to the language rather than a user import.
]

#[section:
    [doc_level:
        [$insert("key"):collection-methods]
        [$insert("title"):Collection methods]
        [$insert("advanced"):[collections_advanced]]

        [collections_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../io/]
        [$insert("previous_title"):Core IO]
        [$insert("next_path"):../math/]
        [$insert("next_title"):Core math]
    ]
]
