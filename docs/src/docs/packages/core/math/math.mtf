`@core/math` provides `Float` constants and helper functions. It belongs to the binding-backed Core package family with no prelude alias, so it requires an explicit dependency clause.

`@core/math` carries Core origin with ExternalBinding backing. See @../../#package-origins-and-backing (Package origins and backing) for origin and backing rules.

### Dependency bindings

Bind individual symbols or the whole namespace:

[codeblock, $code("moth"):
    @core/math PI, sin, clamp

    @core/math

    angle = math.PI / 2.0
    result = math.sin(angle)
]

A namespace alias creates a file-local name:

[codeblock, $code("moth"):
    @core/math as math
]

### Constants

[table:
    [th:
        [: Constant]
        [: Type]
        [: Value]
    ]

    [td:
        [: `PI`]
        [: `Float`]
        [: pi]
    ]

    [td:
        [: `TAU`]
        [: `Float`]
        [: 2 * pi]
    ]

    [td:
        [: `E`]
        [: `Float`]
        [: Euler's number]
    ]

    [td:
        [: `SQRT_2`]
        [: `Float`]
        [: square root of 2]
    ]

    [td:
        [: `LN_2`]
        [: `Float`]
        [: natural log of 2]
    ]

    [td:
        [: `LN_10`]
        [: `Float`]
        [: natural log of 10]
    ]
]

Constants form compile-time `Float` values. They fold into expressions at compile time and do not produce runtime lookups.

### Functions

All functions take `Float` parameters and return `Float`. Parameters stay positional-only. Named arguments lack support because binding-backed packages do not expose source-function features.

[table:
    [th:
        [: Function]
        [: Parameters]
        [: Returns]
    ]

    [td:
        [: `sin(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `cos(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `tan(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `atan2(y, x)`]
        [: `Float, Float`]
        [: `Float`]
    ]

    [td:
        [: `log(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `log2(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `log10(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `exp(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `pow(base, exponent)`]
        [: `Float, Float`]
        [: `Float`]
    ]

    [td:
        [: `sqrt(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `abs(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `floor(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `ceil(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `round(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `trunc(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `min(a, b)`]
        [: `Float, Float`]
        [: `Float`]
    ]

    [td:
        [: `max(a, b)`]
        [: `Float, Float`]
        [: `Float`]
    ]

    [td:
        [: `clamp(x, min, max)`]
        [: `Float, Float, Float`]
        [: `Float`]
    ]

    [td:
        [: `asin(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `acos(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `atan(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `cbrt(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `hypot(x, y)`]
        [: `Float, Float`]
        [: `Float`]
    ]

    [td:
        [: `expm1(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `log1p(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `sinh(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `cosh(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `tanh(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `asinh(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `acosh(x)`]
        [: `Float`]
        [: `Float`]
    ]

    [td:
        [: `atanh(x)`]
        [: `Float`]
        [: `Float`]
    ]
]

All functions keep infallible Moth signatures. A non-finite result still fails
at the shared external `Float` boundary before ordinary Moth code observes it.
The enclosing numeric failure mode decides what happens next: a function with
builtin `Error!` recovers through that error channel, while a non-fallible
function, a custom fallible channel or entry-selected root runtime code traps.
`NaN`, positive infinity and negative infinity never become valid Moth
`Float` values.

### Numerical contract

Angles are radians for `sin`, `cos`, `tan`, `asin`, `acos`, `atan` and
`atan2`. `PI` is the radian measure of a half turn and `TAU` of a full turn.

A mathematical result that is undefined or infinite produces no Moth `Float`.
The shared external `Float` boundary rejects the non-finite host result with
builtin error code 304 before source observes it. The functions with
restricted domains are:

- `sqrt(x)` requires `x >= 0`
- `log(x)`, `log2(x)` and `log10(x)` require `x > 0`
- `log1p(x)` requires `x > -1`
- `asin(x)` and `acos(x)` require `x` between `-1` and `1` inclusive
- `acosh(x)` requires `x >= 1`
- `atanh(x)` requires `x` strictly between `-1` and `1`
- `pow(base, exponent)` requires `base >= 0` when `exponent` is not a whole
  number, and `base` other than zero when `exponent` is negative

Overflow to an infinite result, such as `exp`, `cosh` or `sinh` of a large
argument, fails at the same boundary.

`round(x)` breaks ties toward positive infinity. `round(3.5)` is `4`,
`round(-3.5)` is `-3` and `round(-2.5)` is `-2`.

`clamp(x, min, max)` is the composition `min(max(x, min), max)`. When `min`
exceeds `max` the upper bound wins, so the result is `max`.

`atan2(y, x)` returns the angle of the point `(x, y)` from the positive x
axis, between negative `PI` and `PI`.

`hypot(x, y)` returns the square root of `x * x + y * y` and avoids
intermediate overflow.

The sign of a zero result is not part of this contract.

A result too small to be a normal `Float` stays finite. It becomes a
subnormal value or zero and never fails the finite boundary. Which of the
two occurs is unspecified.

Transcendental results are approximations with target-defined accuracy, so
comparisons use a tolerance. These identities are exact: `exp(0)` is `1`,
`log(1)` is `0`, `log2(8)` is `3`, `log10(100)` is `2` and `pow(2, 3)` is `8`.
An approximating function may still round when its mathematical result is
exactly representable, so a result that happens to be a whole number is not
guaranteed exact: `hypot(20, 99)` need not be exactly `101`. Every constant is
the nearest `Float` to its mathematical value.

### Access and return contracts

All parameters use shared access. No function requires mutable access. Every function returns a fresh `Float` value.

### Backend behaviour

HTML-JS lowers each function to a JavaScript `Math` expression. `clamp` lowers to `Math.min(Math.max(x, min), max)`. Constants fold into the expression at compile time.

HTML-Wasm lacks a Wasm lowering for these functions. See @../../../progress (progress matrix) for current target availability.

### Unsupported source forms

- Direct symbol-path clauses such as `@core/math/sin` face rejection. Use direct selections or a namespace clause.
- Named arguments lack support. Binding-backed calls stay positional-only.
- The package does not expose receiver methods, opaque types or `Error!` channels.

### Deferred surfaces

Deferred math surfaces include integer math helpers, checked overflow modes, broader numeric tower support and non-JS lowerings. See @../../../progress (progress matrix) for current status.

### Related concepts

- For package origin and backing see @../../#package-origins-and-backing (Package origins and backing).
- For dependency-path rules see @../../#dependency-paths (Dependency paths).
- For `Float` language semantics see @../../../numbers (Numbers).
