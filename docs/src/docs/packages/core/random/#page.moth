import @./random {
    content as random_advanced,
}

import @./random-basic {
    content as random_basic,
}

import @styles/docs {
    navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager,
}

page_title #= "Core random"
page_description #= "Random number helpers in the Core package."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../../../ (Documentation) / @../ (Core packages) / Core random

    # Core random

    `@core/random` provides random number helpers. Import it when you need
    random `Float` or `Int` values.
]

#[section:
    [doc_level:
        [$insert("key"):random-helpers]
        [$insert("title"):Random helpers]
        [$insert("advanced"):[random_advanced]]

        [random_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../text/]
        [$insert("previous_title"):Core text]
        [$insert("next_path"):../time/]
        [$insert("next_title"):Core time]
    ]
]