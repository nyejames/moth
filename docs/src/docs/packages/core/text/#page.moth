import @html {center}
import @styles/docs {navbar, title, section, codeblock, theme_head}

page_title #= "Core Text"
page_description #= "Text helper functions for String values."
page_head #= theme_head

#[navbar]
#[title, center: CORE TEXT]

#[section, $md:
    `@core/text` provides helper functions for `String` values.

    Current helpers:
    - `length`
    - `is_empty`
    - `contains`
    - `starts_with`
    - `ends_with`

    [codeblock, $code("moth"):
        import @core/text {length, contains}

        name = "Moth"
        count = length(name)
        has_stalk = contains(name, "stalk")

        io.line([:count=[count] has_stalk=[has_stalk]])
    ]

    On the current HTML JavaScript path, text length follows JavaScript string length behavior.
]
