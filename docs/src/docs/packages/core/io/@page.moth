@io content as io_advanced

@io-basic content as io_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Core IO"
page_description #= "Console output and input polling helpers in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../../../ (Documentation) / @../ (Core packages) / Core IO

    # Core IO

    `io` provides console output and explicit input polling. The prelude exposes
    its namespace without a dependency clause, while templates handle value formatting
    before text crosses the output boundary.
]

#[section:
    [doc_level:
        [$insert("key"):console-and-input]
        [$insert("title"):Console and input]
        [$insert("advanced"):[io_advanced]]

        [io_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../prelude/]
        [$insert("previous_title"):Prelude policy]
        [$insert("next_path"):../collections/]
        [$insert("next_title"):Core collections]
    ]
]
