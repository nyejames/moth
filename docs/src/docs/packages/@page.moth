import @import-paths {
    content as import_paths,
}

import @import-paths-basic {
    content as import_paths_basic,
}

import @grouped-and-namespace-imports {
    content as grouped_and_namespace_imports,
}

import @grouped-and-namespace-imports-basic {
    content as grouped_and_namespace_imports_basic,
}

import @module-visibility {
    content as module_visibility,
}

import @module-visibility-basic {
    content as module_visibility_basic,
}

import @public-reexports {
    content as public_reexports,
}

import @public-reexports-basic {
    content as public_reexports_basic,
}

import @project-local-packages {
    content as project_local_packages,
}

import @project-local-packages-basic {
    content as project_local_packages_basic,
}

import @package-origins-and-backing {
    content as package_origins_and_backing,
}

import @package-origins-and-backing-basic {
    content as package_origins_and_backing_basic,
}

import @external-binding-contracts {
    content as external_binding_contracts,
}

import @external-binding-contracts-basic {
    content as external_binding_contracts_basic,
}

import @styles {
    navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager,
}

page_title #= "Packages and imports"
page_description #= "Import paths, module visibility, project-local packages and package origin and backing."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Packages and imports

    # Packages and imports

    Imports make names visible, module roots define public boundaries and
    packages provide stable named surfaces. Moth keeps those jobs separate.

    Unlike languages that expose any reachable filesystem path, Moth resolves
    source imports from the owning module root and stops at each module facade.
    Project-local support packages share code without opening private paths.
]

#[section:
    [doc_level:
        [$insert("key"):import-paths]
        [$insert("title"):Import paths]
        [$insert("advanced"):[import_paths]]

        [import_paths_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):grouped-and-namespace-imports]
        [$insert("title"):Grouped and namespace imports]
        [$insert("advanced"):[grouped_and_namespace_imports]]

        [grouped_and_namespace_imports_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):module-visibility]
        [$insert("title"):Module visibility]
        [$insert("advanced"):[module_visibility]]

        [module_visibility_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):public-reexports]
        [$insert("title"):Public re-exports]
        [$insert("advanced"):[public_reexports]]

        [public_reexports_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):project-local-packages]
        [$insert("title"):Project-local packages]
        [$insert("advanced"):[project_local_packages]]

        [project_local_packages_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):package-origins-and-backing]
        [$insert("title"):Package origins and backing]
        [$insert("advanced"):[package_origins_and_backing]]

        [package_origins_and_backing_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):external-binding-contracts]
        [$insert("title"):External binding contracts]
        [$insert("advanced"):[external_binding_contracts]]

        [external_binding_contracts_basic]
    ]
]

#[section, $md:
    ## Package reference pages

    - @./core (Core packages)
    - @./builder (Builder packages)
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../project-structure/]
        [$insert("previous_title"):Project structure]
        [$insert("next_path"):../moth-templates/]
        [$insert("next_title"):Moth template files]
    ]
]
