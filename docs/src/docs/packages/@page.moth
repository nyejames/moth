@dependency-paths content as dependency_paths

@dependency-paths-basic content as dependency_paths_basic

@dependency-clauses content as dependency_clauses

@dependency-clauses-basic content as dependency_clauses_basic

@module-visibility content as module_visibility

@module-visibility-basic content as module_visibility_basic

@public-reexports content as public_reexports

@public-reexports-basic content as public_reexports_basic

@project-local-packages content as project_local_packages

@project-local-packages-basic content as project_local_packages_basic

@package-origins-and-backing content as package_origins_and_backing

@package-origins-and-backing-basic content as package_origins_and_backing_basic

@external-binding-contracts content as external_binding_contracts

@external-binding-contracts-basic content as external_binding_contracts_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Packages and dependencies"
page_description #= "Dependency paths, module visibility, project-local packages and package origin and backing."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Packages and dependencies

    # Packages and dependencies

    Dependencies make names visible, module roots define public boundaries and
    packages provide stable named surfaces. Moth keeps those jobs separate.

    Unlike languages that expose any reachable filesystem path, Moth resolves
    source dependencies from the owning module root and stops at each module facade.
    Project-local support packages share code without opening private paths.
]

#[section:
    [doc_level:
        [$insert("key"):dependency-paths]
        [$insert("title"):Dependency paths]
        [$insert("advanced"):[dependency_paths]]

        [dependency_paths_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):dependency-clauses]
        [$insert("title"):Dependency clauses]
        [$insert("advanced"):[dependency_clauses]]

        [dependency_clauses_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):module-visibility]
        [$insert("title"):Module visibility]
        [$insert("advanced"):[module_visibility]]

        [module_visibility_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):public-reexports]
        [$insert("title"):Public re-exports]
        [$insert("advanced"):[public_reexports]]

        [public_reexports_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):project-local-packages]
        [$insert("title"):Project-local packages]
        [$insert("advanced"):[project_local_packages]]

        [project_local_packages_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):package-origins-and-backing]
        [$insert("title"):Package origins and backing]
        [$insert("advanced"):[package_origins_and_backing]]

        [package_origins_and_backing_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):external-binding-contracts]
        [$insert("title"):External binding contracts]
        [$insert("advanced"):[external_binding_contracts]]

        [external_binding_contracts_basic]
    ]
]

#[section, $md:
    ## Package reference pages

    - @./core (Core packages)
    - @./builder (Builder packages)
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../project-structure/]
        [$insert("previous_title"):Project structure]
        [$insert("next_path"):../resources/]
        [$insert("next_title"):Resources and file values]
    ]
]
