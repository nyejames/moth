@type-aliases content as type_aliases

@type-aliases-basic content as type_aliases_basic

@dependency-aliases content as dependency_aliases

@dependency-aliases-basic content as dependency_aliases_basic

@payload-capture-aliases content as payload_capture_aliases

@payload-capture-aliases-basic content as payload_capture_aliases_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Aliases"
page_description #= "Type aliases, dependency aliases and choice payload capture aliases in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Aliases

    # Aliases

    An alias gives an existing type, dependency or payload field a local name. It
    doesn't create a value, conversion or new nominal type.

    Moth keeps `as` narrower than languages with broad renaming or casting
    syntax. The surrounding declaration always determines what gets renamed.
]

#[section:
    [doc_level:
        [$insert("key"):type-aliases]
        [$insert("title"):Type aliases]
        [$insert("advanced"):[type_aliases]]

        [type_aliases_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):dependency-aliases]
        [$insert("title"):Dependency aliases]
        [$insert("advanced"):[dependency_aliases]]

        [dependency_aliases_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):payload-capture-aliases]
        [$insert("title"):Payload capture aliases]
        [$insert("advanced"):[payload_capture_aliases]]

        [payload_capture_aliases_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../constants/]
        [$insert("previous_title"):Constants and compile-time behaviour]
        [$insert("next_path"):../generics/]
        [$insert("next_title"):Generics]
    ]
]
