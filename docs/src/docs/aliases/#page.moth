import @./type-aliases {
    content as type_aliases,
}

import @./type-aliases-basic {
    content as type_aliases_basic,
}

import @./import-aliases {
    content as import_aliases,
}

import @./import-aliases-basic {
    content as import_aliases_basic,
}

import @./payload-capture-aliases {
    content as payload_capture_aliases,
}

import @./payload-capture-aliases-basic {
    content as payload_capture_aliases_basic,
}

import @styles/docs {
    navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager,
}

page_title #= "Aliases"
page_description #= "Type aliases, import aliases and choice payload capture aliases in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Aliases

    # Aliases

    Aliases give an existing thing a better name. They do not create a second value,
    a hidden conversion or a new nominal type.

    Moth keeps `as` narrow: type names, imported names and arm-local payload
    captures.
]

#[section:
    [doc_level:
        [$insert("key"):type-aliases]
        [$insert("title"):Type aliases]
        [$insert("advanced"):[type_aliases]]

        [type_aliases_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):import-aliases]
        [$insert("title"):Import aliases]
        [$insert("advanced"):[import_aliases]]

        [import_aliases_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):payload-capture-aliases]
        [$insert("title"):Payload capture aliases]
        [$insert("advanced"):[payload_capture_aliases]]

        [payload_capture_aliases_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../constants/]
        [$insert("previous_title"):Constants and compile-time behaviour]
        [$insert("next_path"):../generics/]
        [$insert("next_title"):Generics]
    ]
]
