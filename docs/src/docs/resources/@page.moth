@file-paths content as file_paths

@file-paths-basic content as file_paths_basic

@file-values content as file_values

@file-values-basic content as file_values_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Resources and file values"
page_description #= "Naming project files with file paths, and what those paths evaluate to in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Resources and file values

    # Resources and file values

    Projects contain more than source. Images, fonts, stylesheets and data files
    need to reach the output, and the links pointing at them need to be correct
    from wherever they are read.

    Moth handles this with one small surface. A file path names a file and
    evaluates to a `String`. The compiler validates the file and remembers which
    one you meant, until a builder decides where it lands and what the URL says.
]

#[section:
    [doc_level:
        [$insert("key"):file-paths]
        [$insert("title"):File paths]
        [$insert("advanced"):[file_paths]]

        [file_paths_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):file-values]
        [$insert("title"):File values]
        [$insert("advanced"):[file_values]]

        [file_values_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../packages/]
        [$insert("previous_title"):Packages and dependencies]
        [$insert("next_path"):../moth-templates/]
        [$insert("next_title"):Moth template files]
    ]
]
