@collection-literals content as collection_literals

@collection-literals-basic content as collection_literals_basic

@growable-collections content as growable_collections

@growable-collections-basic content as growable_collections_basic

@fixed-collections content as fixed_collections

@fixed-collections-basic content as fixed_collections_basic

@collection-operations content as collection_operations

@collection-operations-basic content as collection_operations_basic

@hash-maps content as hash_maps

@hash-maps-basic content as hash_maps_basic

@styles navbar,
    section,
    language_theme_head,
    doc_level,
    doc_pager

page_title #= "Collections and maps"
page_description #= "Collections, fixed capacity, builtin operations and insertion-ordered maps in Moth."
page_head #= language_theme_head

#[navbar]

#[section, $md:
    @../ (Documentation) / Collections and maps

    # Collections and maps

    Collections store one element type in order. Maps associate one value type
    with scalar keys and preserve insertion order.

    Both use braces because square brackets build Moth templates. Unlike many
    languages, indexing doesn't form part of the source surface. Fallible
    methods such as `get`, `set` and `remove` make bounds and lookup failure
    explicit.
]

#[section:
    [doc_level:
        [$insert("key"):collection-literals]
        [$insert("title"):Collection literals]
        [$insert("advanced"):[collection_literals]]

        [collection_literals_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):growable-collections]
        [$insert("title"):Growable collections]
        [$insert("advanced"):[growable_collections]]

        [growable_collections_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):fixed-collections]
        [$insert("title"):Fixed collections]
        [$insert("advanced"):[fixed_collections]]

        [fixed_collections_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):collection-operations]
        [$insert("title"):Collection operations]
        [$insert("advanced"):[collection_operations]]

        [collection_operations_basic]
    ]
]

#[section:
    [doc_level:
        [$insert("key"):hash-maps]
        [$insert("title"):Hash maps]
        [$insert("advanced"):[hash_maps]]

        [hash_maps_basic]
    ]
]

#[section:
    [doc_pager:
        [$insert("previous_path"):../errors/]
        [$insert("previous_title"):Errors, options and assertions]
        [$insert("next_path"):../templates/]
        [$insert("next_title"):Templates]
    ]
]
